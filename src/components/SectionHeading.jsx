// eslint-disable-next-line react/prop-types
const SectionHeading = ({ title }) => {
  return (
    <div className="text-white text-[30px] text-left font-extrabold mt-10 mb-10">
      {title}
    </div>
  );
};

export default SectionHeading;
